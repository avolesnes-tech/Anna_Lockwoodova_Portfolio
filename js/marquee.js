/* =============================================================
   marquee.js — Pohyblivý text (CSS animácia, JS len pre pause)
   CSS animácia riadi pohyb, tento súbor je rezervovaný
   pre prípadné dynamické pridávanie textu alebo kontrolu.
   ============================================================= */

// Marquee je čisto CSS-driven (animations.css).
// Pauza na hover je tiež CSS (:hover .marquee__inner).
// Tento súbor je tu pre rozšíriteľnosť.

(function () {
  'use strict';

  // Žiadna akcia potrebná — CSS sa stará o všetko.
  // V prípade budúceho rozšírenia (napr. dynamický text zo servera):

  // const marqueeInner = document.querySelector('.marquee__inner');
  // if (!marqueeInner) return;
  // ...

})();
